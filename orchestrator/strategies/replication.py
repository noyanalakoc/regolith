"""
Week 4 placeholder.

Replication plus voting: run each chunk on N replicas with the same seed.
Because the kernel is deterministic, honest replicas agree exactly, so a
disagreement reveals corruption. Correct under bit-flips, at N times the cost.
"""
